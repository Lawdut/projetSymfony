{% extends 'base.html.twig' %}

{% block title %}Hello EspeceController!{% endblock %}

{% block body %}


<div class="example-wrapper">
    <h1>Les espèces </h1>

    <ul>
    {% for espece in especes %}
        <li><a href ="{{ path('afficher_espece', {'id':espece.id})  }}"> {{espece.libelle}}</a> {{espece.description}}</li>
    {% endfor %}
    </ul>
</div>

{% endblock %}
